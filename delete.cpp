#include <string>
#include <fstream>
#include <iostream>
using namespace std;

int main()
{
    string fname="test.txt";
    string line;
    ofstream outfile(fname);
    if(outlfile.is_open)
}
